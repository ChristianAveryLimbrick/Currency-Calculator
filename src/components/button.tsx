




'use client';

import { Button } from 'flowbite-react';

const Button = ({children}) =>{
  return (
    <div className="flex flex-wrap gap-2">
      <Button gradientMonochrome="success">Convert</Button>
 
    </div>
  );
}

export default Button;
